from enum import Enum


class TimeUnit(Enum):
    NANOS = 'NANOS'
    MILLIS = 'MILLIS',
    MICROS = 'MICROS',
    SECONDS = 'SECONDS',
    MINUTES = 'MINUTES',
    HOURS = 'HOURS',
    DAYS = 'DAYS',
