server:
  port: 9000

spring:
  application:
    name: cloudberry
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: cloudberry-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    properties:
      spring:
        json:
          trusted.packages: '*'
  data:
    mongodb:
      host: localhost
      port: 27017
      database: cloudberry
  influx2:
    url: http://localhost:9999/
    username: root
    password: ziemniak
    bucket: cloudberry-logs

---

spring:
  profiles: prod
  kafka:
    bootstrap-servers: kafka-cb:29092
  data:
    mongodb:
      host: cloudberry-cb

---

spring:
  profiles: dev
  kafka:
    bootstrap-servers: localhost:9092

---

spring:
  profiles: olgierd
  influx2:
    org: 05da34a331fc2000
    token: XXdzWLu5TSeSbunFAv5ORgx8M6AnDgEYJknNRw57_vuzXy4aWK_P96_ztKTTlBDQZ2hb_ADr65C9nYtEiFLdhg==

---

spring:
  profiles: michal
  influx2:
    org: halo # todo
    token: policja # todo
